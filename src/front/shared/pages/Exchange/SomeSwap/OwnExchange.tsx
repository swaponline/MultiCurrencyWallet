import CSSModules from 'react-css-modules'
import styles from './index.scss'

function OwnExchange() {
  return <form action=""></form>
}

export default CSSModules(OwnExchange, styles, { allowMultiple: true })
