export = publicKeySelector;
/**
 * Best record selector, for public key records.
 * Simply returns the first record, as all valid public key
 * records are equal.
 *
 * @param {Uint8Array} k
 * @param {Array<Uint8Array>} records
 */
declare function publicKeySelector(k: Uint8Array, records: Array<Uint8Array>): number;
//# sourceMappingURL=public-key.d.ts.map