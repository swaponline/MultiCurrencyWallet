import React from 'react'

import reloadIcon from './images/reload.svg'


const ReloadIcon = ({ className }) => (
  <img className={className} src={reloadIcon} alt="reload icon" />
)

export default ReloadIcon
